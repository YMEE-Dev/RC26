{
  "name": "model-signed-url",
  "version": "1.0.0",
  "description": "Lambda function to generate pre-signed S3 URLs for encrypted 3D model files",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "build": "npm ci --omit=dev && zip -r ../model-signed-url.zip ."
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0"
  }
}
