{%- comment -%} nav-arrow — trailing diagonal arrow for menu voices. size: 'sm' | 'lg' {%- endcomment -%}
{%- assign cls = 'rc-arrow rc-arrow--sm' -%}
{%- if size == 'lg' -%}{%- assign cls = 'rc-arrow rc-arrow--lg' -%}{%- endif -%}
<svg class="{{ cls }}" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M7 17L17 7M8.5 7H17v8.5" stroke="currentColor" stroke-width="1.2"></path></svg>
